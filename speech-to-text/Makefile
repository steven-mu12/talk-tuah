# Makefile for speech to text

# Python interpreter
PYTHON = python3

# Main script
SCRIPT = speech-to-text.py

# Run the speech-to-text transcription
transcribe:
	./$(SCRIPT)

# Make the script executable
chmod:
	chmod +x $(SCRIPT)

# Default target
.DEFAULT_GOAL := transcribe
